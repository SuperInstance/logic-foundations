[package]
name = "logic-foundations"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
repository = "https://github.com/SuperInstance/logic-foundations"
description = "Logic foundations in Rust — propositional logic, predicate logic, CNF, resolution, satisfiability, proof systems"

[dependencies]
serde = { version = "1", features = ["derive"] }
nalgebra = "0.33"
